---
description: Sim 主题颜色为用户配置的默认值，更新代码时不得擅自覆盖或重置
alwaysApply: true
---

# 保留 Sim 主题颜色

用户在 Sim 弹窗里调整的 **theme 颜色** 已写入代码作为默认值，后续任何更新都**不要**再改回去或覆盖。

## 受保护的值（除非用户明确要求修改，否则不要动）

1. **`app/src/constants/nav-theme.ts`**
   - `WATCHLIST_BORDER`：当前为 `#27272d`（Watchlist 外边框）
   - `WATCHLIST_BG`：当前为 `#141415`（Watchlist 背景）

2. **`app/src/app/page.tsx`**
   - App 背景的初始 state：`useState('#101011')`（App 背景）

## 规则

- 做重构、修 bug、加功能时，**不要**把上述常量或初始值改成别的（例如不要改回 `#444444`、`#1C1C1E`、`#111` 等）。
- 只有用户**明确说**要改 theme/颜色/Sim 默认值时，才修改这些地方。
- 若需新增与主题相关的常量，可参考现有值，但不要覆盖上述三个。
